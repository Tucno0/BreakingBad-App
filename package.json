{
  "name": "http-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch server/db.json"
  },
  "devDependencies": {
    "json-server": "^0.17.3",
    "vite": "^4.3.9"
  }
}
